/*!
        File Description: Provides top level configuration for the application.
*/

'use strict';

import {
  IServerConnectionOptions
} from 'hapi';

const config : IServerConnectionOptions = {
  port: 3001
};

export default config;
